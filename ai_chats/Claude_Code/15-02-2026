 Отличные уточнения! Это сложная архитектурная задача. Перед
  планированием мне нужны важные уточнения для правильного
  дизайна:

  🎯 Уточняющие вопросы

  1. Структура задач и связь с параметрами

  - Как задача привязывается к параметру? Например, задача
  "Покраска велосипеда":
    - Привязана к значению параметра (например, "Платформа:
  Велосипед-новый")?
    - Или к нескольким значениям одновременно (например,
  "Ресурс: Маляр 1" И "Платформа: Велосипед-новый")?
    - Или задача существует независимо и по фильтру параметра
  отображается на строке?

  Пример структуры:
  interface Task {
    id: string;
    title: string;
    startDate: Date;
    endDate: Date;
    // Вариант 1: привязана к одному значению параметра
    parameterId: number;
    parameterValue: string;  // "Маляр 1", "Велосипед-новый"

    // Вариант 2: привязана к нескольким параметрам
    // parameterValues: { [parameterId]: string }
  }

  ---
  2. Визуализация задач (Gantt-чарт)

  - Как рендерятся задачи на строке?
    - Горизонтальный прямоугольник (Gantt bar) с длиной,
  зависящей от дат?
    - Или просто иконка/точка в день?
    - Со сколькими деталями: только название, или + статус,
  приоритет?

  Макет строки:
  Строка: "Маляр 1"
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  [Янв]      [Фев]      [Март]     [Апр]
           ┌─────────────────┐
  Задача 1 │ Покраска биков  │ (5-15 март, зеленый, done)
           └─────────────────┘
                          ┌────────┐
  Задача 2 │   Сборка       │ (14-20 март, желтый, in_progress)
           └────────────────┘

  ---
  3. Поведение при сворачивании группы (collapse)

  Когда я сворачиваю группу (например, "Производство"):
  - Задачи дочерних параметров исчезают? (исчезают строки →
  исчезают и задачи)
  - Или задачи дочерних остаются видны, но строки скрыты?
  - Нужна ли агрегация задач? (например, показать общее
  количество открытых задач в родителе)

  ---
  4. Поведение при закреплении (freeze)

  Когда я закрепляю уровень-0 параметр (например,
  "Производство"):
  - Закрепляются все дочерние строки как есть? (например, если
  видны 5 цехов, все 5 закреплены)
  - Или закрепляется только родитель, а дети остаются в
  прокручиваемой части?
  - Должны ли закреплённые задачи быть отделены визуально?
  (например, другой цвет фона)

  FROZEN AREA (закреплено):
  ┌─ Производство ──────────────────────┐
  │  ├─ Сварочный цех (сворачиваемый)   │
  │  │  ├─ Сварщик 1  [════ Задача 1]   │
  │  │  └─ Сварщик 2                    │
  │  └─ Сборка                          │
  └──────────────────────────────────────┘

  SCROLLABLE AREA (прокручивается):
  ┌─ Логистика ─────────────────────────┐
  │  ├─ Склад готовой продукции         │
  │  │  ├─ Кладовщик 1  [═════ Задача]  │
  │  │  └─ Кладовщик 2                  │
  │  └─ Отгрузка                        │
  └──────────────────────────────────────┘

  ---
  5. Как выглядит сетка сейчас vs должна выглядеть

  Сейчас: пустые строки 28px, нет задач
  Нужно:
  - Отобразить Gantt bars (горизонтальные полосы) в
  соответствии с датами?
  - Или просто заполнить ячейки таблицы "дата×параметр" с
  информацией о задаче?
  - Какая высота строки: 28px достаточно, или нужна больше?

  ---
  6. Статус и стилизация задач

  - Какие статусы задач? (planned, in_progress, completed,
  blocked и т.д.)
  - Цвета по статусам? (зелёный = done, жёлтый = in_progress,
  серый = planned)
  - Нужны ли значки приоритета? (важно, не важно)
  - Клик по задаче = редактирование? Или только просмотр?

  ---
  7. Mock-данные для задач

  Нужны ли мне примеры задач для демонстрации? Например:

  const TASKS = [
    {
      id: '1',
      title: 'Сварка рамы',
      parameterId: 2,      // "Сварочный цех"
      parameterValue: 'Сварщик 1',
      startDate: new Date(2026, 0, 5),
      endDate: new Date(2026, 0, 10),
      status: 'completed',
      color: '#22c55e'  // green
    },
    // ... ещё задачи
  ];

  ---
   Предварительный план (ждёмся ответов)

  На основе ваших ответов я спланирую:

  1. Обновление типов (Parameter, Task, Task → Parameter
  mapping)
  2. Изменение логики collapse (как задачи исчезают при
  сворачивании?)
  3. Изменение логики freeze (закрепляется иерархия или только
  родитель?)
  4. Компонент GanttBar (как рендерятся задачи на временной
  оси)
  5. Grid layout (как организовать таблицу "параметр × время")
  6. State management (где хранятся задачи, как
  синхронизируются с коллапсом/фризом)

  ---
  Ответьте на эти вопросы (хотя бы на 1-5), и я создам
  детальный план с примерами кода! 🎯

> 1.  Структура задач и связь с параметрами - реализация
  фильтрами параметр >> начение.

● Спасибо за уточнение! Модель ясна: задача фильтруется по
  parameterId >> parameterValue.

  Значит:
  interface Task {
    id: string;
    title: string;
    startDate: Date;
    endDate: Date;
    status: 'planned' | 'in_progress' | 'completed' | ...;

    // Фильтр: параметр >> значение
    parameterId: number;        // e.g., 2 (Сварочный цех)
    parameterValue: string;     // e.g., "Сварщик 1"

    // При отображении строки "Сварщик 1"
    // → показываются все Task где parameterId=2 &&
  parameterValue="Сварщик 1"
  }

  ---
  Теперь продолжу уточнять оставшиеся вопросы 2-7:

  2️⃣ Визуализация задач (Gantt vs другое)?

  Задачи должны отображаться как:
  - A) Gantt bars — горизонтальные полосы, длина которых
  зависит от (endDate - startDate)?
  - B) Блоки в ячейках — таблица "день × параметр", в каждой
  ячейке информация о задачах в этот день?
  - C) Что-то ещё?

  Пример A (Gantt):
  Строка "Маляр 1" в диапазоне "Янв-Март 2026":
  Янв         Фев         Март
  ║           ║           ║
          ┌─────────────────┐
          │ Покраска биков  │ (5-15 фев, зеленый, done)
          └─────────────────┘
                      ┌──────┐
                      │Сборка│ (10-18 март, желтый, in_prog)
                      └──────┘

  Пример B (таблица ячеек):
           5 фев  6 фев  7 фев ... 10 март
  Маляр 1  Покр.  Покр.  Покр. ... Сбор.
  Маляр 2

  ---
  3️⃣ Поведение при сворачивании группы

  Когда закрываю "Сварочный цех" (level 1):
  - Исчезают строки "Сварщик 1", "Сварщик 2", "Сварщик 3"
  - Должны ли исчезнуть их задачи? ✓ или они остаются видны
  как-то?
  - Нужна ли агрегация? Например, показать in "Сварочный цех"
  количество открытых задач: "(3 активных)"?

  ---
  4️⃣ Закрепление иерархии (freeze)

  Когда я закрепляю "Производство" (level 0):
  - Все видимые дочерние уровни закрепляются?
    - Если развернуто: Производство → Сварка → Сварщики →
  закрепляются все 3 уровня
    - Если свернуто: Производство закреплено, но развёртывание
  происходит в frozen area
  - Или закрепляется только сама "Производство" как заголовок,
  а дети в scrollable area?

  ---
  5️⃣ Высота строки и рендеринг задач

  Сейчас строка = 28px. Это достаточно для одной задачи?
  - Если несколько задач одновременно на одной строке?
  - Нужна ли динамическая высота (если 3 задачи → строка 84px)?
  - Или одна строка = одна параметр-значение, и задачи просто
  слои друг на друга (overlap)?

  ---
  6️⃣ Статусы, цвета, интеракция

  - Какие статусы задач и цвета?
    - planned → серый (#9ca3af)
    - in_progress → жёлтый (#eab308)
    - completed → зелёный (#22c55e)
    - blocked → красный (#ef4444)
    - ещё?
  - Клик по задаче:
    - Открывает modal/popup с деталями?
    - Редактирует task inline?
    - Просто выделяет?
  - Перетаскивание (drag-to-reschedule)?
    - Можно ли тащить task по оси времени, чтобы изменить
  startDate/endDate?

  ---
  7️⃣ Mock-данные для TASKS

  Мне нужны примеры. Предлагаю вот такую структуру:

  const TASKS_MOCK: Task[] = [
    // Задачи для параметра "Сварочный цех" > "Сварщик 1"
    {
      id: 'task-1',
      title: 'Сварка рамы',
      parameterId: 2,        // Сварочный цех
      parameterValue: 'Сварщик 1',
      startDate: new Date(2026, 0, 5),
      endDate: new Date(2026, 0, 10),
      status: 'completed',
      color: '#22c55e'
    },
    {
      id: 'task-2',
      title: 'Сварка крепежей',
      parameterId: 2,
      parameterValue: 'Сварщик 1',
      startDate: new Date(2026, 0, 11),
      endDate: new Date(2026, 0, 15),
      status: 'in_progress',
      color: '#eab308'
    },
    // Задачи для параметра "Платформа" > "Велосипед-новый"
    {
      id: 'task-3',
      title: 'Разработка велосипеда',
      parameterId: 1,        // Платформа (если level 0)
      parameterValue: 'Велосипед-новый',
      startDate: new Date(2026, 0, 1),
      endDate: new Date(2026, 0, 30),
      status: 'in_progress',
      color: '#eab308'
    },
    // ...
  ];

  ---
  Ответьте на вопросы 2-7, и я напишу детальный план с:
  - ✅ Обновленной структурой компонентов
  - ✅ GanttBar компонентом (или Table ячейкой)
  - ✅ Логикой collapse/freeze с задачами
  - ✅ State management
  - ✅ Примерами кода

───────────────────────────────────────────────────────────────
> спланируй на основе Gantt bars
───────────────────────────────────────────────────────────────
  ? for shortcuts